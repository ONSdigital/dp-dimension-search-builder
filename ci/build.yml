---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.13.9

inputs:
  - name: dp-search-builder

outputs:
  - name: build

run:
  path: dp-search-builder/ci/scripts/build.sh
